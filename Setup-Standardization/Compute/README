Compute Setup

Go to the Dom0 machine
1. Import the provided compute image with xe as below
# xe vm-import filename=base_image_compute_new 
2. xe vm-start uuid=<uuid return in above step>
3. xe console uuid=<vm-uuid>

On compute vm
1. Configure the network if needed
2. Clone 'Compute.tgz' in any directory and change directory to Compute
3. run "compute-config.sh" to configure compute node. 
4. When all compute services are running, run the nova network-create command on controller machine
#nova network-create test --fixed-range-v4=192.168.1.0/24 --multi-host=T --bridge=xenbr0 --bridge-interface=eth1 --gateway=192.168.1.254 --dns1=192.168.0.4
Note:  Value of fixed-range-v4 in the above command should be the  range specified in /etc/dhcp/dhcpd.conf file in DHCP server
